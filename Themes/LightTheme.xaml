<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ═══════════════════════════════════════════════════════════════════
         BQE CORE — EXACT LIGHT THEME
         Primary #2c99f0 · Dark #296598 · BG #F9FAFC · Text #1E293B
         Font: Open Sans / Inter · Radius: 8px buttons / 5px components
         Source: BQECoreWebApp/css/themes/light-theme-variables.css
         ═══════════════════════════════════════════════════════════════════ -->

    <SolidColorBrush x:Key="WindowBg"           Color="#F9FAFC"/>

    <!-- ── Sidebar — dark left nav (matches Core screenshot) ─────────────── -->
    <SolidColorBrush x:Key="SidebarBg"          Color="#1E2533"/>
    <SolidColorBrush x:Key="SidebarBorder"      Color="#2A3347"/>
    <SolidColorBrush x:Key="SidebarNavFg"       Color="#94A3B8"/>

    <!-- ── Cards ─────────────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="CardBg"             Color="#FFFFFF"/>
    <SolidColorBrush x:Key="CardBorder"         Color="#D3DAE1"/>
    <Color            x:Key="CardShadowColor"   >#000000</Color>

    <!-- ── Text ──────────────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="PrimaryText"        Color="#1E293B"/>
    <SolidColorBrush x:Key="SubText"            Color="#64748B"/>
    <SolidColorBrush x:Key="MutedText"          Color="#94A3B8"/>

    <!-- ── DataGrid ──────────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="GridRowBg"          Color="#FFFFFF"/>
    <SolidColorBrush x:Key="GridAltRowBg"       Color="#F9FAFC"/>
    <SolidColorBrush x:Key="GridHeaderBg"       Color="#F9FAFC"/>
    <SolidColorBrush x:Key="GridHeaderFg"       Color="#465568"/>
    <SolidColorBrush x:Key="GridBorder"         Color="#E1E8F0"/>
    <SolidColorBrush x:Key="GridFg"             Color="#1E293B"/>

    <!-- ── Inputs ────────────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="InputBg"            Color="#FFFFFF"/>
    <SolidColorBrush x:Key="InputBorder"        Color="#CBD5E1"/>
    <SolidColorBrush x:Key="InputFg"            Color="#1E293B"/>

    <!-- ── Tabs ──────────────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="TabFg"              Color="#64748B"/>
    <SolidColorBrush x:Key="TabSelectedFg"      Color="#2c99f0"/>
    <SolidColorBrush x:Key="TabSelectedBorder"  Color="#2c99f0"/>
    <SolidColorBrush x:Key="TabControlBg"       Color="#FFFFFF"/>
    <SolidColorBrush x:Key="TabHoverBg"         Color="#F9FAFC"/>

    <!-- ── Code / JSON ───────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="CodeBg"             Color="#1E2533"/>
    <SolidColorBrush x:Key="CodeFg"             Color="#e5f4fd"/>

    <!-- ── KPI / Stats ───────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="StatCountFg"        Color="#1E293B"/>
    <SolidColorBrush x:Key="KpiHeaderBg"        Color="#1E2533"/>
    <SolidColorBrush x:Key="KpiHeaderFg"        Color="#FFFFFF"/>
    <SolidColorBrush x:Key="KpiValueFg"         Color="#2c99f0"/>
    <SolidColorBrush x:Key="KpiCardBorder"      Color="#D3DAE1"/>

    <!-- ── Semantic ──────────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="SeparatorFg"           Color="#E1E8F0"/>
    <SolidColorBrush x:Key="StatusLabelFg"         Color="#64748B"/>
    <SolidColorBrush x:Key="InfoBoxBg"             Color="#FFFFFF"/>
    <SolidColorBrush x:Key="CopyBtnBg"             Color="#F1F4F9"/>
    <SolidColorBrush x:Key="CopyBtnBorder"         Color="#CBD5E1"/>

    <!-- ── Quick-fetch chips ─────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="QuickFetchAchBg"       Color="#EDE9FF"/>
    <SolidColorBrush x:Key="QuickFetchAchBorder"   Color="#C4B5FD"/>
    <SolidColorBrush x:Key="QuickFetchAchFg"       Color="#6D28D9"/>
    <SolidColorBrush x:Key="QuickFetchCcBg"        Color="#e5f4fd"/>
    <SolidColorBrush x:Key="QuickFetchCcBorder"    Color="#93C5FD"/>
    <SolidColorBrush x:Key="QuickFetchCcFg"        Color="#296598"/>

    <!-- ── Cards / tiles ─────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="CardBorder2"        Color="#E1E8F0"/>
    <SolidColorBrush x:Key="TileCardBg"         Color="#FFFFFF"/>
    <SolidColorBrush x:Key="PanelTitleFg"       Color="#1E293B"/>

    <!-- ── JSON formatter ────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="JsonInputBg"              Color="#F9FAFC"/>
    <SolidColorBrush x:Key="JsonInputBorder"          Color="#CBD5E1"/>
    <SolidColorBrush x:Key="JsonOutputGreenBg"        Color="#DCFCE7"/>
    <SolidColorBrush x:Key="JsonOutputGreenBorder"    Color="#17A34A"/>
    <SolidColorBrush x:Key="JsonOutputYellowBg"       Color="#FEF3C7"/>
    <SolidColorBrush x:Key="JsonOutputYellowBorder"   Color="#D87706"/>

    <!-- ── Labels ────────────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="LabelFg"            Color="#1E293B"/>
    <SolidColorBrush x:Key="SubLabelFg"         Color="#64748B"/>
    <SolidColorBrush x:Key="HintFg"             Color="#94A3B8"/>

    <!-- ── Info banner ───────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="InfoBannerBg"       Color="#e5f4fd"/>
    <SolidColorBrush x:Key="InfoBannerBorder"   Color="#2c99f0"/>
    <SolidColorBrush x:Key="InfoBannerTitleFg"  Color="#296598"/>
    <SolidColorBrush x:Key="InfoBannerNumFg"    Color="#2c99f0"/>
    <SolidColorBrush x:Key="InfoBannerTextFg"   Color="#465568"/>

    <!-- ── Search card ───────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="SearchCardBg"       Color="#FFFFFF"/>
    <SolidColorBrush x:Key="SearchCardBorder"   Color="#D3DAE1"/>

    <!-- ── Logo badge ────────────────────────────────────────────────────── -->
    <LinearGradientBrush x:Key="LogoBadgeBg" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#2c99f0" Offset="0"/>
        <GradientStop Color="#296598" Offset="1"/>
    </LinearGradientBrush>

    <!-- ── Section headers ───────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="EmailSectionBg"     Color="#EDE9FF"/>
    <SolidColorBrush x:Key="EmailSectionBorder" Color="#C4B5FD"/>
    <SolidColorBrush x:Key="EmailSectionFg"     Color="#6D28D9"/>

    <SolidColorBrush x:Key="TxnSectionBg"       Color="#e5f4fd"/>
    <SolidColorBrush x:Key="TxnSectionBorder"   Color="#2c99f0"/>
    <SolidColorBrush x:Key="TxnSectionFg"       Color="#296598"/>

    <SolidColorBrush x:Key="DisbSectionBg"      Color="#DCFCE7"/>
    <SolidColorBrush x:Key="DisbSectionBorder"  Color="#17A34A"/>
    <SolidColorBrush x:Key="DisbSectionFg"      Color="#166535"/>

    <!-- ── Webhook panel ─────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="WebhookPanelBg"       Color="#DCFCE7"/>
    <SolidColorBrush x:Key="WebhookPanelBorder"   Color="#17A34A"/>
    <SolidColorBrush x:Key="WebhookConfigBg"      Color="#FFFFFF"/>
    <SolidColorBrush x:Key="WebhookConfigBorder"  Color="#17A34A"/>
    <SolidColorBrush x:Key="WebhookConfigFg"      Color="#1E293B"/>

    <!-- ── Info strips ───────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="InfoStripGreenBorder"     Color="#17A34A"/>
    <SolidColorBrush x:Key="InfoStripBlueBorder"      Color="#2c99f0"/>
    <SolidColorBrush x:Key="InfoStripGreenFg"         Color="#1E293B"/>
    <SolidColorBrush x:Key="InfoStripBlueFg"          Color="#1E293B"/>
    <SolidColorBrush x:Key="InfoStripGreenBtnBg"      Color="#DCFCE7"/>
    <SolidColorBrush x:Key="InfoStripGreenBtnHover"   Color="#BBF7D0"/>
    <SolidColorBrush x:Key="InfoStripBlueBtnBg"       Color="#e5f4fd"/>
    <SolidColorBrush x:Key="InfoStripBlueBtnHover"    Color="#e7f3fd"/>
    <SolidColorBrush x:Key="InfoStripBlueBtnBg2"      Color="#e5f4fd"/>

    <!-- ── Orange / amber ────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="OrangePanelBg"       Color="#FEF3C7"/>
    <SolidColorBrush x:Key="OrangePanelBorder"   Color="#D87706"/>
    <SolidColorBrush x:Key="OrangePanelFg"       Color="#1E293B"/>

    <!-- ── DB config ─────────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="DbConfigBg"          Color="#FFFFFF"/>
    <SolidColorBrush x:Key="DbConfigBlueBg"      Color="#e5f4fd"/>
    <SolidColorBrush x:Key="DbConfigBorder"      Color="#D3DAE1"/>
    <SolidColorBrush x:Key="DbConfigBlueBorder"  Color="#2c99f0"/>

    <!-- ── Summary bar ───────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="SummaryBarBg"        Color="#F9FAFC"/>
    <SolidColorBrush x:Key="SummaryBarBorder"    Color="#D3DAE1"/>

    <!-- ── Test row states ───────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="TestRowPassBg"       Color="#DCFCE7"/>
    <SolidColorBrush x:Key="TestRowFailBg"       Color="#FEE2E1"/>
    <SolidColorBrush x:Key="TestRowRunningBg"    Color="#FEF3C7"/>

    <!-- ── Surface / convenience ─────────────────────────────────────────── -->
    <SolidColorBrush x:Key="DarkSurface"         Color="#F9FAFC"/>
    <SolidColorBrush x:Key="DarkSurface2"        Color="#FFFFFF"/>
    <SolidColorBrush x:Key="DarkBorder"          Color="#E1E8F0"/>
    <SolidColorBrush x:Key="DarkText"            Color="#1E293B"/>
    <SolidColorBrush x:Key="DarkSubText"         Color="#64748B"/>
    <SolidColorBrush x:Key="DarkAccent"          Color="#2c99f0"/>
    <SolidColorBrush x:Key="DarkBtnBg"           Color="#F1F4F9"/>
    <SolidColorBrush x:Key="DarkBtnBgHover"      Color="#E1E8F0"/>
    <SolidColorBrush x:Key="DarkScrollBg"        Color="#F9FAFC"/>

    <!-- ── Env toggle ────────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="EnvBtnInactiveBg"     Color="#F1F4F9"/>
    <SolidColorBrush x:Key="EnvBtnInactiveBorder" Color="#CBD5E1"/>
    <SolidColorBrush x:Key="EnvBtnInactiveFg"     Color="#344056"/>

    <SolidColorBrush x:Key="FgDefault"           Color="#1E293B"/>


    <!-- ═══════════════════════════════════════════════════════════════════
         SCROLLBAR — Core style: 8px, #CBD5E1 thumb
         ═══════════════════════════════════════════════════════════════════ -->

    <Style x:Key="CoreScrollThumb" TargetType="Thumb">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="IsTabStop"             Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border x:Name="Bd" Background="#CBD5E1" CornerRadius="4"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#94A3B8"/>
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#2c99f0"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CoreScrollPageBtn" TargetType="RepeatButton">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="IsTabStop"             Value="False"/>
        <Setter Property="Focusable"             Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border Background="Transparent"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="CoreVScrollBar" TargetType="ScrollBar">
        <Border Background="Transparent" Width="8" Padding="2,4">
            <Track x:Name="PART_Track" IsDirectionReversed="True">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource CoreScrollPageBtn}" Command="ScrollBar.PageUpCommand"/>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource CoreScrollThumb}" Margin="0,2"/>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource CoreScrollPageBtn}" Command="ScrollBar.PageDownCommand"/>
                </Track.IncreaseRepeatButton>
            </Track>
        </Border>
    </ControlTemplate>

    <ControlTemplate x:Key="CoreHScrollBar" TargetType="ScrollBar">
        <Border Background="Transparent" Height="8" Padding="4,2">
            <Track x:Name="PART_Track">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource CoreScrollPageBtn}" Command="ScrollBar.PageLeftCommand"/>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource CoreScrollThumb}" Margin="2,0"/>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource CoreScrollPageBtn}" Command="ScrollBar.PageRightCommand"/>
                </Track.IncreaseRepeatButton>
            </Track>
        </Border>
    </ControlTemplate>

    <Style TargetType="ScrollBar">
        <Setter Property="OverridesDefaultStyle"  Value="True"/>
        <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Width"    Value="8"/>
                <Setter Property="Template" Value="{StaticResource CoreVScrollBar}"/>
            </Trigger>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Height"   Value="8"/>
                <Setter Property="Template" Value="{StaticResource CoreHScrollBar}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="ScrollViewer">
        <Setter Property="CanContentScroll"            Value="False"/>
        <Setter Property="PanningMode"                 Value="VerticalOnly"/>
        <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollViewer">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <ScrollContentPresenter Grid.Column="0" Grid.Row="0"
                                                CanContentScroll="{TemplateBinding CanContentScroll}"/>
                        <ScrollBar x:Name="PART_VerticalScrollBar"
                                   Grid.Column="1" Grid.Row="0" Orientation="Vertical"
                                   Value="{TemplateBinding VerticalOffset}"
                                   Maximum="{TemplateBinding ScrollableHeight}"
                                   ViewportSize="{TemplateBinding ViewportHeight}"
                                   Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                        <ScrollBar x:Name="PART_HorizontalScrollBar"
                                   Grid.Column="0" Grid.Row="1" Orientation="Horizontal"
                                   Value="{TemplateBinding HorizontalOffset}"
                                   Maximum="{TemplateBinding ScrollableWidth}"
                                   ViewportSize="{TemplateBinding ViewportWidth}"
                                   Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!-- ═══════════════════════════════════════════════════════════════════
         CONTROL STYLES — Exact BQE Core Light
         Font: Open Sans / Inter · Radius: 8px (buttons) 5px (fields)
         ═══════════════════════════════════════════════════════════════════ -->

    <Style TargetType="Window">
        <Setter Property="Foreground" Value="#1E293B"/>
        <Setter Property="FontFamily" Value="Open Sans, Inter, Segoe UI, Arial"/>
        <Setter Property="FontSize"   Value="14"/>
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="Foreground"                     Value="#1E293B"/>
        <Setter Property="FontFamily"                     Value="Open Sans, Inter, Segoe UI, Arial"/>
        <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
        <Setter Property="TextOptions.TextRenderingMode"  Value="ClearType"/>
    </Style>

    <!-- TextBox: 5px radius, #CBD5E1 border, focus #38BDF8 glow -->
    <Style TargetType="TextBox">
        <Setter Property="Background"      Value="#FFFFFF"/>
        <Setter Property="Foreground"      Value="#1E293B"/>
        <Setter Property="BorderBrush"     Value="#CBD5E1"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CaretBrush"      Value="#2c99f0"/>
        <Setter Property="SelectionBrush"  Value="#38BDF8"/>
        <Setter Property="FontFamily"      Value="Open Sans, Inter, Segoe UI, Arial"/>
        <Setter Property="Padding"         Value="8,6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="5">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Padding="{TemplateBinding Padding}"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                      CanContentScroll="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="#38BDF8"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="#94A3B8"/>
                        </Trigger>
                        <Trigger Property="IsReadOnly" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#F9FAFC"/>
                            <Setter Property="Foreground"                 Value="#64748B"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.50"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="PasswordBox">
        <Setter Property="Background"      Value="#FFFFFF"/>
        <Setter Property="Foreground"      Value="#1E293B"/>
        <Setter Property="BorderBrush"     Value="#CBD5E1"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CaretBrush"      Value="#2c99f0"/>
        <Setter Property="SelectionBrush"  Value="#38BDF8"/>
        <Setter Property="FontFamily"      Value="Open Sans, Inter, Segoe UI, Arial"/>
        <Setter Property="Padding"         Value="8,6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="PasswordBox">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="5">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Padding="{TemplateBinding Padding}"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="#38BDF8"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="#94A3B8"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.50"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBox: 5px radius, Core dropdown shadow -->
    <Style TargetType="ComboBox">
        <Setter Property="Background"  Value="#FFFFFF"/>
        <Setter Property="Foreground"  Value="#1E293B"/>
        <Setter Property="BorderBrush" Value="#CBD5E1"/>
        <Setter Property="FontFamily"  Value="Open Sans, Inter, Segoe UI, Arial"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton"
                                      Background="{TemplateBinding Background}"
                                      BorderBrush="{TemplateBinding BorderBrush}"
                                      BorderThickness="1"
                                      Focusable="False"
                                      IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay,
                                                  RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="TBd"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="5">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="24"/>
                                            </Grid.ColumnDefinitions>
                                            <Path Grid.Column="1" HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Data="M0,0 L4,5 L8,0 Z"
                                                  Fill="#94A3B8"/>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="TBd" Property="BorderBrush" Value="#94A3B8"/>
                                        </Trigger>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="TBd" Property="BorderBrush" Value="#38BDF8"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ContentPresenter x:Name="ContentSite"
                                          IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          Margin="10,0,28,0"
                                          VerticalAlignment="Center"/>
                        <TextBox x:Name="PART_EditableTextBox"
                                 Visibility="Hidden" IsReadOnly="{TemplateBinding IsReadOnly}"
                                 Margin="8,0,28,0" VerticalAlignment="Center"
                                 Background="Transparent" Foreground="#1E293B"
                                 BorderThickness="0"/>
                        <Popup x:Name="Popup" Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True" Focusable="False"
                               PopupAnimation="Fade">
                            <Grid x:Name="DropDown"
                                  MinWidth="{TemplateBinding ActualWidth}"
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <!-- Core dropdown shadow: 0px 12px 16px -4px rgba(16,24,40,0.1) -->
                                <Border Background="#FFFFFF" BorderBrush="#E1E8F0"
                                        BorderThickness="1" CornerRadius="5" Padding="4">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="20" ShadowDepth="6"
                                                          Opacity="0.10" Color="#101828"/>
                                    </Border.Effect>
                                    <ScrollViewer>
                                        <StackPanel IsItemsHost="True"
                                                    KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="ContentSite"          Property="Visibility" Value="Hidden"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Background" Value="#FFFFFF"/>
        <Setter Property="Foreground" Value="#1E293B"/>
        <Setter Property="Padding"    Value="10,8"/>
        <Setter Property="FontFamily" Value="Open Sans, Inter, Segoe UI, Arial"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#f5f5f5"/>
                            <Setter Property="Foreground"                 Value="#1E293B"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#e5f4fd"/>
                            <Setter Property="Foreground"                 Value="#296598"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="TabControl">
        <Setter Property="Background"      Value="#FFFFFF"/>
        <Setter Property="BorderBrush"     Value="#E1E8F0"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>

    <!-- TabItem: Core blue underline, Open Sans -->
    <Style TargetType="TabItem">
        <Setter Property="Background"  Value="Transparent"/>
        <Setter Property="Foreground"  Value="#64748B"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Padding"     Value="16,10"/>
        <Setter Property="FontSize"    Value="14"/>
        <Setter Property="FontFamily"  Value="Open Sans, Inter, Segoe UI, Arial"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="TabBorder" Background="Transparent"
                            BorderThickness="0,0,0,2" BorderBrush="Transparent"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter ContentSource="Header"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                          TextBlock.Foreground="{TemplateBinding Foreground}"
                                          TextBlock.FontWeight="SemiBold"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="TabBorder" Property="BorderBrush" Value="#2c99f0"/>
                            <Setter Property="Foreground" Value="#2c99f0"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="#296598"/>
                            <Setter TargetName="TabBorder" Property="Background" Value="#F9FAFC"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Button: 8px radius (primary/secondary), Core blue solid primary -->
    <Style TargetType="Button">
        <Setter Property="Background"      Value="#F1F4F9"/>
        <Setter Property="Foreground"      Value="#1E293B"/>
        <Setter Property="BorderBrush"     Value="#CBD5E1"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor"          Value="Hand"/>
        <Setter Property="Padding"         Value="20,8"/>
        <Setter Property="FontFamily"      Value="Open Sans, Inter, Segoe UI, Arial"/>
        <Setter Property="FontSize"        Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background"  Value="#F9FAFC"/>
                            <Setter TargetName="Bd" Property="BorderBrush" Value="#94A3B8"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background"  Value="#E1E8F0"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.50"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- PrimaryButton style — Core solid blue: #2c99f0 bg, white text, 8px radius -->
    <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background"  Value="#2c99f0"/>
        <Setter Property="Foreground"  Value="#FFFFFF"/>
        <Setter Property="BorderBrush" Value="#2c99f0"/>
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="#1E293B"/>
        <Setter Property="FontFamily" Value="Open Sans, Inter, Segoe UI, Arial"/>
    </Style>

    <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="#1E293B"/>
        <Setter Property="FontFamily" Value="Open Sans, Inter, Segoe UI, Arial"/>
    </Style>

    <Style TargetType="DatePicker">
        <Setter Property="Background"  Value="#FFFFFF"/>
        <Setter Property="Foreground"  Value="#1E293B"/>
        <Setter Property="BorderBrush" Value="#CBD5E1"/>
        <Setter Property="FontFamily"  Value="Open Sans, Inter, Segoe UI, Arial"/>
    </Style>

    <!-- DataGrid: Core table style — #F9FAFC header, #E1E8F0 lines -->
    <Style TargetType="DataGrid">
        <Setter Property="Background"               Value="#FFFFFF"/>
        <Setter Property="Foreground"               Value="#1E293B"/>
        <Setter Property="BorderBrush"              Value="#D3DAE1"/>
        <Setter Property="BorderThickness"          Value="0"/>
        <Setter Property="RowBackground"            Value="#FFFFFF"/>
        <Setter Property="AlternatingRowBackground" Value="#F9FAFC"/>
        <Setter Property="HorizontalGridLinesBrush" Value="#E1E8F0"/>
        <Setter Property="VerticalGridLinesBrush"   Value="Transparent"/>
        <Setter Property="ColumnHeaderHeight"       Value="36"/>
        <Setter Property="RowHeight"                Value="38"/>
        <Setter Property="FontFamily"               Value="Open Sans, Inter, Segoe UI, Arial"/>
        <Setter Property="FontSize"                 Value="13"/>
        <Setter Property="SelectionUnit"            Value="FullRow"/>
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background"      Value="#F9FAFC"/>
        <Setter Property="Foreground"      Value="#465568"/>
        <Setter Property="BorderBrush"     Value="#D3DAE1"/>
        <Setter Property="BorderThickness" Value="0,0,0,1.5"/>
        <Setter Property="Padding"         Value="12,0"/>
        <Setter Property="FontWeight"      Value="SemiBold"/>
        <Setter Property="FontSize"        Value="12"/>
        <Setter Property="FontFamily"      Value="Open Sans, Inter, Segoe UI, Arial"/>
        <Setter Property="Height"          Value="36"/>
    </Style>

    <Style TargetType="DataGridCell">
        <Setter Property="Foreground"      Value="#1E293B"/>
        <Setter Property="BorderBrush"     Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding"         Value="4,0"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="#e5f4fd"/>
                <Setter Property="Foreground" Value="#296598"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="DataGridRow">
        <Setter Property="Background"      Value="#FFFFFF"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Margin"          Value="0"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="#e7f3fd"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#f5f5f5"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Label">
        <Setter Property="Foreground" Value="#1E293B"/>
        <Setter Property="FontFamily" Value="Open Sans, Inter, Segoe UI, Arial"/>
    </Style>

    <Style TargetType="GroupBox">
        <Setter Property="Foreground"  Value="#1E293B"/>
        <Setter Property="BorderBrush" Value="#D3DAE1"/>
        <Setter Property="FontFamily"  Value="Open Sans, Inter, Segoe UI, Arial"/>
    </Style>

    <!-- ToolTip: Core dark tooltip — dark bg, white text -->
    <Style TargetType="ToolTip">
        <Setter Property="Background"      Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding"         Value="0"/>
        <Setter Property="HasDropShadow"   Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolTip">
                    <Border Background="#1E293B" CornerRadius="5"
                            Padding="12,8" BorderBrush="#344056" BorderThickness="1">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="16" ShadowDepth="4"
                                              Opacity="0.15" Color="#101828"/>
                        </Border.Effect>
                        <TextBlock Text="{TemplateBinding Content}"
                                   Foreground="#FFFFFF" FontSize="13"
                                   FontFamily="Open Sans, Inter, Segoe UI, Arial"
                                   TextWrapping="Wrap" MaxWidth="440"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ContextMenu">
        <Setter Property="Background"  Value="#FFFFFF"/>
        <Setter Property="Foreground"  Value="#1E293B"/>
        <Setter Property="BorderBrush" Value="#E1E8F0"/>
        <Setter Property="FontFamily"  Value="Open Sans, Inter, Segoe UI, Arial"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="#FFFFFF" BorderBrush="#E1E8F0"
                            BorderThickness="1" CornerRadius="5" Padding="4">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="20" ShadowDepth="6"
                                              Opacity="0.10" Color="#101828"/>
                        </Border.Effect>
                        <ItemsPresenter/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="#1E293B"/>
        <Setter Property="FontFamily" Value="Open Sans, Inter, Segoe UI, Arial"/>
        <Setter Property="Padding"    Value="12,8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}" Margin="0,1">
                        <ContentPresenter ContentSource="Header"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#f5f5f5"/>
                            <Setter Property="Foreground"                 Value="#296598"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Separator">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Border Height="1" Background="#E1E8F0" Margin="8,3"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
